#!/usr/bin/env ruby
require 'pry'
require 'fileutils'

args      = ARGV
action    = args[0]
file_path = args[1]
puts "You ran Hyde with these arguments: #{args}"

def new_project(path)
  FileUtils.cp_r( 'bin/template/.', path)
end

def check_path(path)
  Dir.exist?(path)
end

def create_skeleton(action, path)
  raise ArgumentError if check_path(path)
  if action == 'new'
    new_project(path)
    # populate created directory with 'skeleton'
  end
end

create_skeleton(action, file_path)


if __FILE__ == $0

end
