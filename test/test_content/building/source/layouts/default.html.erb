<!-- <html>
  <head>
    <% css_paths.each do |path| %>
    <link rel="stylesheet" type="text/css" href="<%= path %>"><% end %>
    <title>Our Site <% if front_matter.has_key?(:title) %>- <%=front_matter[:title]%><% end %></title>
  </head>
  <body>
    <%= html %>
  </body>
</html> -->

<html>
  <head>
    <% css_paths.each do |path| %>
    <link rel="stylesheet" type="text/css" href="<%= path %>"><% end %>
    <title>Our Site <% if front_matter.has_key?(:title) %>- <%=front_matter[:title]%><% end %></title>
  </head>
  <body>
    <% if front_matter.has_key?(:title) %><header><h1 id="title"><%=front_matter[:title]%></h1></header><% end %>
    <%= html %>
  </body>
</html>
